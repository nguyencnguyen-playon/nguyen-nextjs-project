'use client'
import { LayoutContent, CreatePosts } from "@/components";

export const CreatePostView = () => {
    return (
        <LayoutContent>
            <CreatePosts />
        </LayoutContent>
    );
}
